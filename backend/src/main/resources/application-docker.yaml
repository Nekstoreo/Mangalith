spring:
  datasource:
    url: jdbc:postgresql://postgres:5432/mangalith_db
    username: mangalith_user
    password: mangalith_password
  
  redis:
    host: redis
    port: 6379

server:
  port: 8080
